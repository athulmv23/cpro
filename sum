#include<stdio.h>
 
int main() {
   int a, b, sum;
 
   printf("\nEnter two no: ");//input
   scanf("%d %d", &a, &b);
 
   sum = a + b;//calculation
 
   printf("Sum : %d", sum);//output
 
   return(0);
}
